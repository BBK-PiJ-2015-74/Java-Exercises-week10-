package examples;

// this is a functional interface as it has only method defined
// the implementation of the method will be defined by a lambda expression

public interface MyNumber {
	double getValue();
}
